#include <bits/stdc++.h>

using namespace std;

long long solution(int n, vector<int> times) {
    long long answer = 0;
    
    int start = 0;
    int end = n - 1;

    while(start <= end) {
        int mid = (start + end) / 2;
    }
    
    return answer;
}