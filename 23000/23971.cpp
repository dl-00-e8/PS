#include <bits/stdc++.h>

using namespace std;

#define endl "\n"

int H, W, N, M;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> H >> W >> N >> M;

    return 0;
}